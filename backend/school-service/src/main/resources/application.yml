server:
  port: 8083

spring:
  application:
    name: school-service
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://${ul.db.host}:${ul.db.port}/up_learn?serverTimezone=Asia/Shanghai&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: ${ul.db.pw}
  cloud:
    nacos:
      server-addr: localhost:8858
      username: nacos
      password: nacos
      discovery:
        ephemeral: true

mybatis-plus:
  global-config:
    db-config:
      update-strategy: not-null
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

logging:
  level:
    '[com.yukimomo]': debug
  pattern:
    dateformat: HH:mm:ss:SSS
  file:
    path: logs/school-service.log

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
  group-configs:
    - group: default
      paths-to-match: /**
      packages-to-scan: com.yukimomo.school.controller

knife4j:
  enable: true
  setting:
    language: zh_cn
