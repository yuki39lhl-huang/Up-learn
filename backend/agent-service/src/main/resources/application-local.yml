# profile=local：IDEA 本机启动，连 Docker 映射端口（与 user/practice-service 一致）
spring:
  cloud:
    nacos:
      discovery:
        ip: 127.0.0.1

ul:
  db:
    host: localhost
    port: 3308
    pw: 1234
  redis:
    host: localhost
    port: 6380
  agent:
    rag:
      # 向量存现有 Redis 8（6380，已带 RediSearch）；embedding 优先环境变量 DASHSCOPE_API_KEY，否则本地 AllMiniLm
      enabled: true
      ingest-on-startup: true
      embedding: auto
