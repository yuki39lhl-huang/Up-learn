---
description: Git 提交时勿还原 application-local.yml 本机密钥
alwaysApply: true
---

# 本机 local 配置与 Git

- **不要**对 `**/application-local.yml`（尤其 `user-service`）执行 `git checkout` / `git restore` / 覆盖清空，即使用户本机填了 OSS AccessKey。
- 提交时：**不要** `git add` 带真实密钥的 `application-local.yml`；只 stage 其它安全文件即可。
- 若该文件出现在 `git status` 且含密钥：保持工作区不动，排除出本次 commit；可提醒用户勿推送密钥。
- 用户本机 OSS 用于头像等功能；清空会导致本地上传失败。
